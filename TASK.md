# 完整首轮任务

音频处理组件需要将分块输入转换到有理采样率，并在结束时正确排空滤波尾部。请从零实现“有理采样率的分块 FIR 重采样库”。

1. 实现实数单通道重采样，互质1<=L,M<=64，FIR h长度1<=K<=257；输入样本有限且绝对值<=1，系数有限且绝对值<=16。不负责滤波器设计、音频文件或声卡。
2. 定义u[nL]=x[n]，其他位置为0；z[t]=sum(j=0..K-1,h[j]*u[t-j])，y[q]=z[qM]。N>0仅输出0<=qM<=(N-1)L+K-1；N=0输出空序列，域外输入为0。
3. 累计收到N>0样本时，Feed只输出尚未输出且qM<=min(NL-1,(N-1)L+K-1)的样本；N=0不输出。Finish按完整定义补齐尾部，重复Finish为空成功，结束后Feed报错；Reset恢复初始状态。
4. 用多相系数与有限历史，不构造L倍上采样数组或保存全部历史。记录累计输入/输出及缓冲样本数，上界与K,L相关；计数用int64并在溢出前拒绝。非法块整体拒绝且状态不变。
5. 用短数组直接上采样卷积参考验证单切点与固定种子分块，误差<=1e-10*(1+abs(reference))；包括L=4,M=1,K=1,N=1时只输出一个样本。演示分块一致与尾部，不做长音频性能平台。

验收重点：K<L输出域、空块/空输入、L=M=1、升降采样、冲激、Finish尾部、坏块原子拒绝、Reset。

工程约束：全新0-1核心库，仅用Go 1.26.5标准库，在Windows原生离线运行，无第三方依赖、外部服务、Docker或前后端。交付实现、聚焦上述边界的测试及简短README；演示命令`go run ./cmd/demo`用小案例在约8秒内打印正常结果和一种真实失败，测试命令`go test ./... -count=1 -timeout=60s`。固定种子随机验证只用少量小样本，避免长压测和额外平台功能，不限制正常纠错所需工具调用。
一次首轮完成，不要求用户追加“继续”。只操作当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区；不添加Actions/CI/部署/Dependabot、不远程推送，不硬编码演示或虚报验证。

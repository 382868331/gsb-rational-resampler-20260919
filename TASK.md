# 完整首轮任务

音频样本分块到达时，重采样结果应当与整段处理一致。请实现有理采样率的 FIR 重采样库，重点处理块边界、输出时机和结束时的尾部样本。

实现实数单通道重采样，互质1<=L,M<=64，FIR h长度1<=K<=257；输入样本有限且绝对值<=1，系数有限且绝对值<=16。不负责滤波器设计、音频文件或声卡。

定义u[nL]=x[n]，其他位置为0；z[t]=sum(j=0..K-1,h[j]*u[t-j])，y[q]=z[qM]。N>0仅输出0<=qM<=(N-1)L+K-1；N=0输出空序列，域外输入为0。

累计收到N>0样本时，Feed只输出尚未输出且qM<=min(NL-1,(N-1)L+K-1)的样本；N=0不输出。Finish按完整定义补齐尾部，重复Finish为空成功，结束后Feed报错；Reset恢复初始状态。

用多相系数与有限历史，不构造L倍上采样数组或保存全部历史。记录累计输入/输出及缓冲样本数，上界与K,L相关；计数用int64并在溢出前拒绝。非法块整体拒绝且状态不变。

用短数组直接上采样卷积参考验证单切点与固定种子分块，误差<=1e-10*(1+abs(reference))；包括L=4,M=1,K=1,N=1时只输出一个样本。演示分块一致与尾部，不做长音频性能平台。

测试里请覆盖这些情况：K<L输出域、空块/空输入、L=M=1、升降采样、冲激、Finish尾部、坏块原子拒绝、Reset。

使用 Go 1.26.5 标准库，代码要能在 Windows 原生环境离线运行。范围就是这个库，不需要第三方依赖、外部服务、Docker 或前后端。请一并写好测试和简短的 README，说明接口及运行方法。

用 `go run ./cmd/demo` 运行演示，在约 8 秒内展示一个正常结果和一个实际触发的失败；用 `go test ./... -count=1 -timeout=60s` 运行测试。随机验证采用固定种子和少量小样本即可，不做长时间压力测试，也不扩展额外的平台功能。

请在本轮完成实现和验证，不要等我回复“继续”。正常排错需要的工具调用不设次数限制。操作范围限于当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区。不要添加 Actions、CI、部署或 Dependabot，也不要向远端推送。演示结果应由代码实际计算；最后如实说明验证结果，未运行或未通过的检查也请写明。

# 完整首轮任务

请从零实现“有理采样率的分块 FIR 重采样库”，用于数字信号处理 / 音频内核中的本地可复用模块。

1. 从零实现实数单通道有理重采样库，给定互质正整数 L,M<=64 和有限 FIR 系数 h[0..K-1]，1<=K<=257；不负责滤波器设计，不需要声卡、音频文件或第三方 DSP 库。
2. 数学定义：输入 x 长 N，上采样 u[t] 在 t=nL 时为 x[n]，其他为0；z[t]=sum(h[j]*u[t-j])，输出 y[q]=z[qM]。N>0 时输出所有满足0<=qM<=(N-1)L+K-1的 q；N=0 输出空序列，区间外输入为0。
3. Feed 接受任意大小块，只输出已由已知输入确定且尚未输出的样本；Finish 输出剩余尾部并结束，重复 Finish 不重复输出，结束后 Feed 返回错误；Reset 恢复初始状态。
4. 使用多相系数和有限历史缓冲，禁止显式构造 L 倍上采样数组或保存全部历史；公开累计输入/输出计数及缓冲样本数，缓冲上界与 K,L 相关而非总输入长。检查 NaN/Inf 和计数溢出，坏块整体拒绝且状态不变。
5. 用直接上采样+卷积的短数组参考实现交叉验证所有单切点及随机分块，绝对/相对误差阈值写清。演示分块与一次性结果相同以及 Finish 尾部。

验收重点：L=M=1、L>M与L<M、K小于L、单样本、空块、冲激、尾部、非法浮点、Reset。

工程范围：这是全新核心库的0-1实现，初始仓库仅有运行骨架，没有业务实现。仅使用Go 1.26.5标准库，在Windows原生环境离线运行；无需安装第三方依赖。不接数据库、中间件、云API、外部程序，不使用Docker，不制作前后端或通用CLI。
交付：实现可复用库、聚焦上述语义的自动化测试和README；go run ./cmd/demo须在约8秒内运行确定性小案例，打印关键正常结果和至少一种真实边界失败，便于录制十几秒视频。测试命令为go test ./... -count=1 -timeout=60s。先阅读本仓库README和TASK.md，简短计划后集中实现，再运行针对性测试与演示；避免反复通读仓库、无关探索和无必要的长篇文档，发现实际问题时正常修复验证，不设硬性工具调用截断。
只能操作当前仓库内文件；不读取用户配置、密钥、环境变量清单或其他工作区。不要新增GitHub Actions、CI、部署、Dependabot或远程推送。一次首轮完成，不要求用户追加“继续”；合理实现取舍写入README。不要为通过测试删除需求、硬编码示例输出或宣称未实际执行的验证通过。
